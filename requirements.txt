# Install with:  .venv/bin/python -m pip install -r requirements.txt
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<7.0

# Not an optional extra: Kobo and BookBub are Cloudflare-gated on the TLS
# fingerprint and render client-side, so without a browser a default run writes
# four metadata files instead of six. Selenium stays a *soft* dependency in code --
# imported lazily on the first rendered fetch -- so a machine without it (or without
# Chrome/Firefox) warns and degrades rather than failing. You do need a real browser
# installed; Selenium 4.6+ fetches the matching driver itself.
selenium>=4.20,<5.0

# pytest is only needed to run tests/.
pytest>=8.0
